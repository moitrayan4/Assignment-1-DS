/*

Algorithm:
~ Create an empty array (Unique)
~ Loop through original array
~ If element which is in original array is not in Unique then append to array
~ Print Unique
~ End

*/


/*


Pseudocode:

RemoveDuplicates(arr,n):
    Unique <- empty array
    for i <- 0 to n-1 do
        if arr[i] not in Unique then append arr[i] to Unique
    end for Loop
    return Unique
end

*/